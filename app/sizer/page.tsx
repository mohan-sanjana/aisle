export const metadata = {
  title: "Sizer — Aisle",
  description:
    "Quantify your AI inference workload and get a server-spec recommendation across three scenarios.",
};

// Foundation: placeholder. Filled in by Sizer UI agent (depends on Sizer calc engine).
export default function SizerPage() {
  return (
    <div className="container mx-auto max-w-3xl px-4 py-12">
      <h1 className="text-h1">Sizer</h1>
      <p className="mt-4 text-slate-600">
        Sizer wizard — coming soon. A seven-step workflow that produces a
        server-spec recommendation across Baseline / Burst / Resilient
        scenarios will live here.
      </p>
    </div>
  );
}
